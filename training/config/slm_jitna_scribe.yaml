# Delentia SLM — The Scribe (jitna-scribe-v0.5) LoRA Configuration
# Purpose: Delta Engine 262K Context Compression & Memory Timeline Summarization
# Base Model: Qwen2.5-27B-Instruct (Jitna v0.5 Engine)

model:
  base_model: "unsloth/Qwen2.5-32B-Instruct-bnb-4bit"
  tokenizer: "unsloth/Qwen2.5-32B-Instruct-bnb-4bit"
  max_seq_length: 16384
  dtype: "bfloat16"
  load_in_4bit: true

lora:
  r: 64                         # Higher rank 64 for complex context compaction precision
  lora_alpha: 128               # α = 2r for stable learning
  lora_dropout: 0
  bias: "none"
  use_rslora: true
  target_modules:
    - "q_proj"
    - "k_proj"
    - "v_proj"
    - "o_proj"
    - "gate_proj"
    - "up_proj"
    - "down_proj"
  task_type: "CAUSAL_LM"

training:
  dataset_path: "datasets/processed/v0.5/jitna_scribe_pairs_v05.parquet"
  dataset_split: "train"
  validation_split: 0.05
  max_samples: null

  per_device_train_batch_size: 2
  gradient_accumulation_steps: 4

  learning_rate: 5.0e-5
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.05
  num_train_epochs: 5

  bf16: true
  fp16: false
  optim: "adamw_8bit"
  weight_decay: 0.01
  max_grad_norm: 0.3

  output_dir: "models/checkpoints/jitna_scribe_v05"
  save_strategy: "epoch"
  save_total_limit: 3
  logging_steps: 10

  evaluation_strategy: "epoch"
  load_best_model_at_end: true
  metric_for_best_model: "eval_loss"

chat_template: |
  {% for message in messages %}
  {% if message['role'] == 'system' %}{{ '<|im_start|>system\n' + message['content'] + '<|im_end|>\n' }}
  {% elif message['role'] == 'cognitive_state' %}{{ '<|im_start|>cognitive_state\n' + message['content'] + '<|im_end|>\n' }}
  {% elif message['role'] == 'user' %}{{ '<|im_start|>user\n' + message['content'] + '<|im_end|>\n' }}
  {% elif message['role'] == 'assistant' %}{{ '<|im_start|>assistant\n' + message['content'] + '<|im_end|>\n' }}
  {% endif %}
  {% endfor %}
  {% if add_generation_prompt %}{{ '<|im_start|>assistant\n' }}{% endif %}

pillar_type: "scribe"
adapter_name: "jitna_scribe_v0.5"
adapter_save_path: "models/adapters/v0.5/jitna_scribe_v0.5"
version: "v0.5"

mlflow:
  experiment_name: "delentia-slm-scribe-v05"
  tracking_uri: "https://delentia-delentia-agent-monitor.hf.space"
  log_model: true

target_metrics:
  compression_ratio_avg: 4.5    # avg 4.5x context compression
  information_retention: 0.96    # >= 96% key facts retained post-compression
