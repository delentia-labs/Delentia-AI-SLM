# Delentia SLM — The Router (jitna-router-v0.5.1) LoRA Configuration
# Purpose: Intent Classification & D/I/A Parameter Assignment
# Base Model: Qwen/Qwen3.6-27B (DRR Edition)
# DRR Applied: r=8 (was 32), modules=q+v only (was 7), Est. size ~75 MB (lightest!)

model:
  base_model: "Qwen/Qwen3.6-27B"
  tokenizer: "Qwen/Qwen3.6-27B"
  max_seq_length: 8192
  dtype: "bfloat16"
  load_in_4bit: true

lora:
  r: 8                          # DRR: 32→8. Classification is the simplest task; r=8 is ideal
  lora_alpha: 16                # α = 2r (RSLoRA standard ratio)
  lora_dropout: 0
  bias: "none"
  use_rslora: true
  target_modules:
    - "q_proj"                  # Controls WHERE model attends
    - "v_proj"                  # Controls WHAT model outputs
  # REMOVED: k_proj, o_proj, gate_proj, up_proj, down_proj
  # Reason: Routing/classification is simplest task; r=8 + q,v only = lightest adapter
  task_type: "CAUSAL_LM"

training:
  dataset_path: "datasets/processed/v0.5/jitna_router_pairs_v05.parquet"
  dataset_split: "train"
  validation_split: 0.05
  max_samples: null

  per_device_train_batch_size: 2
  gradient_accumulation_steps: 4

  learning_rate: 5.0e-5
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.05
  num_train_epochs: 5

  bf16: true
  fp16: false
  optim: "adamw_8bit"
  weight_decay: 0.01
  max_grad_norm: 0.3

  output_dir: "models/checkpoints/jitna_router_v05"
  save_strategy: "epoch"
  save_total_limit: 3
  logging_steps: 10

  evaluation_strategy: "epoch"
  load_best_model_at_end: true
  metric_for_best_model: "eval_loss"

chat_template: |
  {% for message in messages %}
  {% if message['role'] == 'system' %}{{ '<|im_start|>system\n' + message['content'] + '<|im_end|>\n' }}
  {% elif message['role'] == 'cognitive_state' %}{{ '<|im_start|>cognitive_state\n' + message['content'] + '<|im_end|>\n' }}
  {% elif message['role'] == 'user' %}{{ '<|im_start|>user\n' + message['content'] + '<|im_end|>\n' }}
  {% elif message['role'] == 'assistant' %}{{ '<|im_start|>assistant\n' + message['content'] + '<|im_end|>\n' }}
  {% endif %}
  {% endfor %}
  {% if add_generation_prompt %}{{ '<|im_start|>assistant\n' }}{% endif %}

pillar_type: "router"
adapter_name: "jitna_router_v0.5"
adapter_save_path: "models/adapters/v0.5/jitna_router_v0.5"
version: "v0.5"

mlflow:
  experiment_name: "delentia-slm-router-v05"
  tracking_uri: "https://delentia-delentia-agent-monitor.hf.space"
  log_model: true

target_metrics:
  routing_accuracy: 0.98         # >= 98% correct routing decision
  fdia_parameter_mae: 0.02       # MAE <= 0.02 on D, I, A values
